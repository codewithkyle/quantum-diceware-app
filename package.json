{
  "scripts": {
    "build": "npm run build:client && npm run preview",
    "build:client": "esbuild ./src/main.ts --bundle --outfile=public/main.js",
    "preview": "serve ./public"
  },
  "dependencies": {
    "@codewithkyle/supercomponent": "^1.0.0",
    "esbuild": "^0.9.0",
    "lit-html": "^1.3.0",
    "tooltipper": "^0.1.3"
  },
  "devDependencies": {
    "serve": "^11.3.2"
  }
}
